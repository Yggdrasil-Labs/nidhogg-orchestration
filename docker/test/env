# 测试环境配置
SPRING_PROFILES_ACTIVE=test
RESTART_POLICY=unless-stopped

# Valhalla Auth 服务配置
AUTH_HTTP_PORT=8081
AUTH_SERVICE_PORT=20880
AUTH_JAVA_OPTS=-Xms128m -Xmx256m -XX:+UseSerialGC -XX:MaxRAM=512m

# Valhalla User 服务配置
USER_HTTP_PORT=8082
USER_SERVICE_PORT=20881
USER_JAVA_OPTS=-Xms128m -Xmx256m -XX:+UseSerialGC -XX:MaxRAM=512m

# Valhalla User Admin 前端服务配置
FRONTEND_PORT=8080

# Dubbo 配置
DUBBO_REGISTRY_ADDRESS=nacos://nacos-standalone-derby:8848
DUBBO_NAMESPACE=test
